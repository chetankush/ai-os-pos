{
  "name": "mehfil",
  "version": "0.0.1",
  "private": true,
  "description": "Mehfil — AI-native restaurant operating system",
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "format": "biome format --write .",
    "check": "biome check ."
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.2",
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.0"
  }
}
